name: Deploy Poulina AI Hub to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    name: Build production artifact
    runs-on: ubuntu-latest
    env:
      HAS_SUPABASE_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN != '' && 'true' || 'false' }}
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy edge functions
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          if [ -z "$SUPABASE_ACCESS_TOKEN" ]; then
            echo "::error title=Edge function deploy blocked::SUPABASE_ACCESS_TOKEN is missing, so zabbix-auth would stay on the old deployed code and SSO would fail. Add the token in GitHub Actions secrets and rerun this workflow."
            exit 1
          fi
          supabase functions deploy zabbix-auth --project-ref duqxzfyuhdmsnweclnka --no-verify-jwt
          supabase functions deploy zabbix-connector --project-ref duqxzfyuhdmsnweclnka --no-verify-jwt
          supabase functions deploy sso-redeem --project-ref duqxzfyuhdmsnweclnka --no-verify-jwt
          supabase functions deploy incident-ai --project-ref duqxzfyuhdmsnweclnka --no-verify-jwt
          supabase functions deploy ask-stream --project-ref duqxzfyuhdmsnweclnka --no-verify-jwt

      - name: Verify deployed edge functions are reachable
        run: |
          for fn in zabbix-auth zabbix-connector sso-redeem incident-ai ask-stream; do
            for i in $(seq 1 30); do
              status=$(curl -s -o /tmp/${fn}.json -w "%{http_code}" -X OPTIONS https://duqxzfyuhdmsnweclnka.supabase.co/functions/v1/${fn} \
                -H "Origin: https://poulina-hub-spark.lovable.app" \
                -H "Access-Control-Request-Method: POST" \
                -H "Access-Control-Request-Headers: authorization,apikey,content-type")
              if [ "$status" = "200" ] || [ "$status" = "204" ]; then
                echo "${fn} reachable"
                break
              fi
              if [ "$i" -eq 30 ]; then
                echo "${fn} not reachable after deploy (HTTP ${status})"
                cat /tmp/${fn}.json
                exit 1
              fi
              sleep 2
            done
          done
          redeem_health=$(curl -s -o /tmp/sso-redeem-health.json -w "%{http_code}" "https://duqxzfyuhdmsnweclnka.supabase.co/functions/v1/sso-redeem?health=1" \
            -H "Origin: https://poulina-hub-spark.lovable.app")
          if [ "$redeem_health" != "200" ]; then
            echo "sso-redeem health check failed (HTTP ${redeem_health})"
            cat /tmp/sso-redeem-health.json
            exit 1
          fi
          auth_health=$(curl -s -o /tmp/zabbix-auth-health.json -w "%{http_code}" "https://duqxzfyuhdmsnweclnka.supabase.co/functions/v1/zabbix-auth?health=1" \
            -H "Origin: https://poulina-hub-spark.lovable.app")
          if [ "$auth_health" != "200" ] || ! grep -q "zabbix-auth-sso-token-mint-v3" /tmp/zabbix-auth-health.json; then
            echo "zabbix-auth deployed version check failed (HTTP ${auth_health})"
            cat /tmp/zabbix-auth-health.json
            exit 1
          fi

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint
        continue-on-error: true

      - name: Type-check
        run: bunx tsc --noEmit
        continue-on-error: true

      - name: Build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
          VITE_SUPABASE_PROJECT_ID: ${{ secrets.VITE_SUPABASE_PROJECT_ID }}
        run: bun run build

      - name: Add CNAME for custom domain
        run: echo "poulinaihub.younesblg.com" > dist/CNAME

      - name: Add SPA 404 fallback
        run: cp dist/index.html dist/404.html

      - name: Disable Jekyll
        run: touch dist/.nojekyll

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
